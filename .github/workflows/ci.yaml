name: ci-instructions
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    # strategy:
    #   matrix:
    #     node-version: [14.x]
  steps:
    - name: Checkout
    - name: Prepare
      with:
        context: ./
        file: ./Dockerfile
        target: test